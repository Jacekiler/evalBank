version: "3"
services:
  creditcontroller:
    build: "./credit"
    ports:
      - "8080:8080"
    depends_on:
      - postgresdb
      - productcontroller
      - customercontroller

  customercontroller:
    build: "./customer"
    ports:
      - "8081:8081"
    depends_on:
      - postgresdb

  productcontroller:
    build: "./product"
    ports:
      - "8082:8082"
    depends_on:
      - postgresdb

  postgresdb:
    container_name: postgresdb
    image: postgres:latest
    environment:
      POSTGRES_DB: "testdb"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "admin"
    expose:
      - "5432"
    ports:
    - "5432:5432"
